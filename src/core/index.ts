/**
 * Core engine module — scheduler, monitor, booker, event bus.
 *
 * This module will be implemented in Phase 3.
 * For now, it re-exports relevant types used by the core engine.
 */

export type {
  Snipe,
  Watch,
  SnipeStatus,
  WatchStatus,
  TimeSlot,
  BookingResult,
  TimeRange,
} from '../types/index.js';
